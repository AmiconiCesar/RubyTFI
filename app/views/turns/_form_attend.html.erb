<%= form_with(model: @turn) do |form| %>
   <% if @turn.errors.any? %>
    <div class="alert alert-danger" role="alert">
      <strong>🚫Atención hay errores:</strong>
      <ul>
        <% @turn.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


  <div class="container">
    <div class="form-group">  
       
       <div>
            <h5><strong> Sucursal Nombre: <%= @turn.bank_branch.to_string %> </strong></h5>
            <strong> Cliente: </strong> <%= @turn.user.username %><br> 
            <strong> Fecha: </strong> <%= I18n.l(@turn.date, format: "%A, %d-%m-%y") %><br>
            <strong> Horario: </strong> <%= @turn.time.strftime("%H:%M") %><br>
            <strong> Motivo: </strong> <%= @turn.reason %>
       </div>
             
       <div>
          <strong> <%= form.label :comment, style: "display: block" %> </strong>
          <%= form.text_field :comment %>
       </div> 

       <div>
         <br> 
         <%= form.submit "Registrar Atención", :class => "btn btn-outline-success"  %>
       </div>
    </div>
  </div>     
<% end %>