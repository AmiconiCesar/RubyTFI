<%= form_with(model: [ @bank_branch, @schedule ]) do |form| %>
   <% if @schedule.errors.any? %>
    <div class="alert alert-danger" role="alert">
      <strong>🚫Atención hay errores:</strong>
      <ul>
        <% @schedule.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


  <div class="container">
    <div class="form-group">  
       <div>
        <%= form.label :weekday, style: "display: block"%>
        <%= form.select :weekday, options_for_select( Schedule.weekdays.keys,  disabled: :Domingo,
              selected: @schedule.weekday) %>
       
       <div>
        <%= form.label :start_time, style: "display: block" %>
        <%= form.time_field :start_time %>
       </div>

       <div>
        <%= form.label :end_time, style: "display: block" %>
        <%= form.time_field :end_time %>
       </div>

       <div>
         <br> 
         <%= form.submit "Guardar Horario", :class => "btn btn-outline-success"  %>
       </div>
    </div>
  </div>     
<% end %>
